# LangChain.js 常见错误排错指南

## 1. 模块导入错误
**方案：** 确保使用 `@langchain/core` 而非 `langchain` 导入核心组件

## 2. LCEL 链接类型不匹配
**方案：** 确保每个 `.pipe()` 的输入输出类型兼容

## 3. DocumentLoader 加载失败
**方案：** 检查文件路径、编码、依赖包是否安装

## 4. 向量存储查询无结果
**方案：** 确认文档已成功添加，Embedding 模型配置正确
